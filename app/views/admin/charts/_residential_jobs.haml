- [:day, :week, :month].each do |range|
  :javascript
    $(document).ready(function(){
      $.ajax({
        url: '/en/api/v1/charts/residential_jobs?range=' + '#{range}',
        crossDomain: true,
        success: function(data){
          drawColumnChart(
            "#{range}_jobs_chart",
            data,
            {
              chartArea: { width: '65%' },
              title: 'Residential Jobs per #{range}',
              width: window.innerWidth * 0.4
            }
          )
        }
      });
    });
