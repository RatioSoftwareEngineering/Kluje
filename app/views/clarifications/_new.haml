= form_for [:commercial, @clarification = Clarification.new], :html => { :multipart => true } do |f|
  .form-group
    = f.hidden_field :job_id, value: @job.id
    = f.text_area :question, :class => 'form-control non-border', :rows => '4', :required => true
  .padding-bottom-15.text-right
    = submit_tag _('Submit your Question '), class: 'btn btn-danger ', :disable_with => _("Submiting")

