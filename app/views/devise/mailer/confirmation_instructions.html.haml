%p= _('Dear %s,') % @email

%p= _('Thank you for using Kluje.com!')

%p
  = _('Click on the following link to verify your email address.')
%p{align: 'center'}
  = link_to _('Click to verify'), confirmation_url(@resource, confirmation_token: @token, locale: locale), style: 'color: #fcb61a; font-weight: 600;'
